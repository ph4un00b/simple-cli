export default `// make sure you match the same url path
// as for your pages, if not 'npm run build' will not run!
export const url = "/___name___/styles.css";

export default () =>
  \`@tailwind base;
@tailwind components;
@tailwind utilities;\`;`